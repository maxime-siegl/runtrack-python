# function open permet d'ouvrir un fichier (premier parametre = nom, deuxieme parametre pour faire une action dedans (r, a w, x))

# f = open(file, mode)
# print(f.read())

#il faut imperativement close un fichier une fois le traitement fini
# f.close()

# pour ecrire à l'interieur f.write('')

